import { FirstMayusPipe } from './first-mayus.pipe';

describe('FirstMayusPipe', () => {
  it('create an instance', () => {
    const pipe = new FirstMayusPipe();
    expect(pipe).toBeTruthy();
  });
});
