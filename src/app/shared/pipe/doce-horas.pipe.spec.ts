import { DoceHorasPipe } from './doce-horas.pipe';

describe('DoceHorasPipe', () => {
  it('create an instance', () => {
    const pipe = new DoceHorasPipe();
    expect(pipe).toBeTruthy();
  });
});
